'use client';
// The "Viewing as: Student / Alumni / College" demo switcher is retired now
// that real auth is in place. This component is kept as a no-op so existing
// imports across pages don't need to change.
export default function RoleSwitcher() {
  return null;
}
